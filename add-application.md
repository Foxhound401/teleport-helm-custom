## Add application to teleport

To add application you need to aware the following:

- type=app
- app-name
- app-uri

normally if you not familiar with the setup you should create the token with the `ttl` around 2 - 3 hours

